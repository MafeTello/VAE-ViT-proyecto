services:
  bae-vit:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "7860:7860"
    volumes:
      - .:/app                    # ⬅️ MONTAR TODO EL PROYECTO
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONPATH=/app
